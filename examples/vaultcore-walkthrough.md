# Vaultcore Walkthrough

This walk-through keeps the domain vocabulary close to the data instead of burying it in prose.

| Case | Focus | Score | Lane |
| --- | --- | ---: | --- |
| baseline | trust boundary | 167 | ship |
| stress | claim drift | 193 | ship |
| edge | replay exposure | 202 | ship |
| recovery | policy width | 158 | ship |
| stale | trust boundary | 185 | ship |

Start with `edge` and `recovery`. They create the widest contrast in this repository's fixture set, which makes them better review anchors than the middle cases.

The useful comparison is `replay exposure` against `policy width`, not the raw score alone.
